import { modalStyles, payButtonStyles as payButtonModalStyles } from "./modal";
import {
  mapStyles,
  parkingFloatingStyles,
  payButtonStyles,
  headerStyles,
} from "./map";

module.exports = {
  modalStyles,
  mapStyles,
  parkingFloatingStyles,
  payButtonStyles,
  payButtonModalStyles,
  headerStyles,
};
